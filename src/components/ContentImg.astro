---
import { Picture } from "@astrojs/image/components";
const { src, alt, w } = Astro.props     

// const widths = [ 320, 640, 960, 1280, 1600, 1920, 2240, 2560 ];
const widths = [ 320, 640, 768, 1024, 1280, 1600 ];
const sizes = "(min-width: 48em) 48em, 100vw";

		// widths={[600, 900, 1200, 1536]}
		// sizes="(max-width: 800px) 90vw, 1536px"
		// formats={["avif", "webp", "jpg"]}
//sizes="(max-width: 1024px) 100vw, 50vw"
//sizes="(max-width: 480px) 50vw, (max-width: 1200px) 800px, 100vw
//FallBack  640;

---

<Picture 
        class={`mx-auto p-8`}
        src={src} 
        alt={alt}
        widths={widths}
        sizes={sizes}
        formats={["avif", "webp", "jpg"]}
        />