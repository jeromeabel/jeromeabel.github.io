---
import { Icon } from 'astro-icon'

import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'a'> {
  icon: string;
  label: string;
}

const { href, label, icon, ...props } = Astro.props;

---
<a href={href} title={label}><Icon class="w-8" name={icon} /></a>
