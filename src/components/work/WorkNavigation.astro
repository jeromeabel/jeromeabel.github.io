---
import Icon from 'astro-icon';
const aStyle =
  'flex gap-4 max-w-fit px-6 items-center text-lg h-14 bg-transparent hover:bg-white transition-all text-black border border-dashed prose-a:border-none border-black rounded-full';
const iconStyle = 'stroke-current fill-none h-6';
const { prev, next } = Astro.props;
---

<div class="flex justify-between pt-12 hover:prose-a:border-dashed">
  <div class="">
    {
      prev ? (
        <a href={prev?.slug} class={aStyle}>
          <Icon name="ph:arrow-left-light" class={iconStyle} />
          {prev?.data.title}
        </a>
      ) : null
    }
  </div>

  <div class="self-end">
    {
      next ? (
        <a href={next?.slug} class={aStyle}>
          {next?.data.title}
          <Icon name="ph:arrow-right-light" class={iconStyle} />
        </a>
      ) : null
    }
  </div>
</div>
