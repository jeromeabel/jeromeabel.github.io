---
title: Figma: dispose of 4 undecided Magnet-DS pages
created: 2026-08-11
---

Decide the fate of 4 pages left undecided in `Magnet-DS-v1.0`
(file key `ihWIWmvtQPTWgUxlrVjC2c`) by `magnet-ds-docs-v1` Task 8: `📐 Decisions`
(`2716:4244`, content frame `design-decisions` `2729:4244`), `Page 8`
(`2678:23308`), `Page 9` (`2678:32354`), `Page 11` (`2678:34657`). Each holds
real, unreviewed content — a live check found `2716:4244` has 1 child (a
1440×4913 `design-decisions` frame with 7 sub-frames), `Page 8` has 12
top-level frames (an old typography/token specimen set), `Page 9` has 5
(naming suggests commercial UI-kit onboarding/template scaffolding), and
`Page 11` has 33 (reads as generic plugin/instructional boilerplate) — not
the "empty since creation" / "0 children" the plan's own audit assumed. They
sit outside the plan's `📚 Docs` target end-state and were deliberately left
untouched (D3 non-destructive: no deletion without a verified verdict).

Why undecided: full detail in `.specs/02_archives/magnet-ds-docs-v1/notes.md`
(`## Task 8` section) and `.superpowers/sdd/magnet-ds-docs-v1/task-8-report.md`.

Disposition options, per page:
1. Rename into the `🗄 Backup — <source>` scheme (if genuinely prior work
   worth keeping — `Page 8`'s typography/token specimens and
   `design-decisions`'s content are the likeliest candidates).
2. Visually confirm each is disposable boilerplate/template residue and
   delete it explicitly (not inferred from names alone).

Ref: .specs/02_archives/magnet-ds-docs-v1/notes.md (Task 8 section)
Size: S
